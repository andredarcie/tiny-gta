Original prompt: Imite o mini mapa, do GTA Vice City, faça igual.

Progress:
- Confirmed `js/hud.js` already contains the Vice City-style radar implementation: circular clipped map, rotating background, fixed center player arrow, and square blips clamped to the edge.
- Updated `css/style.css` so the minimap HUD shell is circular and dark instead of a rectangular neon panel.
- Ran the web-game Playwright client against `http://localhost:5173`, started gameplay via `#title`, and inspected `output/web-game/shot-1.png`; the minimap appears circular in gameplay.
- Ran `node --check js\hud.js`; syntax check passed.
- Compared against Vice City gameplay radar references from web image search. Updated the radar shell to use the closer black outer edge plus pink/lilac ring and canvas highlight. Re-ran Playwright and inspected `output/web-game/shot-1.png`.
- Renamed the game to Tiny GTA in the HTML title and title-screen logo.
- Translated player-facing text in the main modular game to English: title screen, HUD labels, prompts, missions, Diego dialog, radio labels, car names, and police/status messages.
- Replaced the old standalone `tiny-gta.html` with a redirect to `index.html` so it no longer exposes the outdated Portuguese UI.
- Re-ran syntax checks for the touched JS modules and verified gameplay screenshot `output/web-game/shot-1.png` shows English HUD/mission text.
- Reworked third-person controls: mouse look now uses a separate camera rig with yaw, pitch clamp, shoulder offset, and camera-relative WASD movement. Vertical mouse is non-inverted by default, with `cameraRig.invertY` available if needed.
- Added weapon gameplay: gun pickup, `E` prompt to pick it up, left-click shooting, ray hits on pedestrians/cars, car explosions, wanted increases, shot tracers, and a rotating gun pickup in the world.
- Updated the minimap to rotate from `cameraRig.yaw`, so it follows the mouse/camera direction instead of the vehicle/player heading.
- Added weapon HUD: pistol panel appears only after pickup, shows current ammo and max ammo, highlights low ammo, and shooting consumes the limited 90-round ammo pool.
- User explicitly prohibited Playwright testing for future turns; do not run Playwright unless they explicitly ask for it again.
- Researched third-person shooter aiming practices and updated weapon handling toward the common camera-centered reticle model: visible crosshair while armed/on foot, target color feedback, camera-center aim ray, muzzle-offset tracer, shot recoil pulse, impact rings, and slight camera shake.
- Ran `node --check` for `js/weapons.js`, `js/hud.js`, and `js/state.js`; all passed.

TODO:
- User is manually testing gameplay; adjust weapon feel, camera signs, or controls based on feedback.
- Playtest manually because automated Playwright validation is intentionally skipped per user instruction in this file.
- Current request: improve shooting mechanics with a visible held gun, physical bullet object leaving the gun muzzle, bullets killing street NPCs with blood puddles, and cars exploding only after 4 bullet hits.
- Implemented current request in `js/weapons.js`, `js/pedestrians.js`, `js/player.js`, and `index.html`: held pistol on the player, bullets spawned from the muzzle with visible projectile/trails, NPC bullet kills with blood puddles, blood on car-hit deaths when bodies land, and car bullet damage that explodes on the 4th hit.
- Bumped title build badge to `BUILD 3` and weapons module appends `◆ BULLET`; fresh build should show `BUILD 3 ◆ CAM-R ◆ BULLET`.
- Validation: ran `node --check js/weapons.js`, `node --check js/pedestrians.js`, and `node --check js/player.js`; all passed. Playwright/browser automation was intentionally not run because of the prior user restriction recorded above.
- Current request: on mobile, camera must never auto-recenter when the player stops touching the look stick. Changed `updateCamera` so touch input disables recenter completely. Bumped badge to `BUILD 4`; fresh build should show `BUILD 4 ◆ CAM-R ◆ BULLET`.
- Current request: add realistic police radio subtitles in English for crimes. Added `js/police-radio.js`, HUD/CSS subtitle box, crime-specific `addWanted(..., crime)` wiring, and immediate typewriter playback from `reportPoliceCrime`. Follow-up fix after user reported subtitles did not appear: radio playback no longer depends on the main loop to dequeue messages, the box has higher z-index, firing the gun directly reports `gunfire`, and badge is now `BUILD 6`; fresh build should show `BUILD 6 ◆ CAM-R ◆ BULLET ◆ RADIO`.
- Follow-up request: police radio text should look and sit like movie subtitles, not a HUD panel. Removed the panel styling from `#police-radio`, moved it to bottom-center, changed it to white subtitle text with heavy black outline/shadow and a small yellow `[POLICE RADIO]` speaker label. Badge is now `BUILD 7`.
- Follow-up request: police subtitles need more detail such as "a man in blue firing shots in the plaza" and "a man in blue stole a pink car." Updated `js/police-radio.js` to build contextual lines with suspect description, approximate area (`central plaza`, `city intersection`, `beachfront`, downtown side), and vehicle description (`pink car`, `marked police cruiser`, etc.). Badge is now `BUILD 8`.
- Current request: make game characters have arms and legs, and move legs while walking. Added limb meshes and `animatePed` in `js/entities.js`, wired walking animation for player and street NPCs in `js/player.js` and `js/pedestrians.js`, and bumped badge to `BUILD 9`. Validation: `node --check js/entities.js`, `js/player.js`, `js/pedestrians.js`, and `js/weapons.js` passed. Playwright remains skipped per user restriction.
- Follow-up bug: browser reported `entities.js` did not export `animatePed`, likely a stale cache mix where new `player.js` loaded with old `entities.js`. Cache-busted all local `entities.js` imports to `entities.js?v=10`, changed `animatePed` calls to optional namespace access in player/pedestrians, and bumped badge to `BUILD 10`. Syntax checks passed for entities/player/pedestrians/main/police/traffic/diego.
- Current request: add randomized mouth, nose, eyes, head variation, beard/hair to NPCs. Updated `makePed` in `js/entities.js` with random facial features and head scale; cache-busted entity imports to `entities.js?v=11`; badge is now `BUILD 11`. Syntax checks passed for entities/player/pedestrians/main/police/traffic/diego. Playwright remains skipped per user restriction.
- Current request: improve character modeling overall. Reworked `makePed` proportions with separate torso, chest/shoulders, hip, neck, head, arms with hands, legs with shoes, varied skin/pants/shoe colors, and kept random face/hair/beard features. Cache-busted entity imports to `entities.js?v=12`; badge is now `BUILD 12`. Syntax checks passed for entities/player/pedestrians/main/police/traffic/diego. Playwright remains skipped per user restriction.
- Current request: when the player has a gun, the arm should stretch out and hold the weapon at the hand. Updated `js/weapons.js` so armed mode poses the right arm forward, offsets the left arm, places the pistol at the right hand, and computes bullet muzzle after updating the arm/gun transform. Cache-busted weapons imports to `weapons.js?v=13`; badge is now `BUILD 13`. Syntax checks passed for weapons/main/input. Playwright remains skipped per user restriction.
- Follow-up bug: bullet appeared stuck in the air after the arm/gun pose change. Updated `aimRay` to use a stable horizontal camera direction, spawn the bullet slightly in front of the muzzle, and advance the bullet immediately on creation so it visibly leaves the gun. Cache-busted weapons imports to `weapons.js?v=14`; badge is now `BUILD 14`. Syntax checks passed for weapons/main/input. Playwright remains skipped per user restriction.
- Audio (BUILD 41): realistic synthesized gunshot replacing the 1200Hz square blip. New `gunshot(vol)` in audio.js layers: highpass noise crack (~2.4-3.1kHz, 50ms), bandpass blast body (~520-660Hz, 130ms), lowpass street-reverb tail (900Hz, 420ms), and a sine thump pitch-dropping 150→45Hz — all through a tanh(2.4x) WaveShaper saturator on a shared `shotBus`, plus a 90ms lowpassed feedback-delay echo (buildings). Random freq variation per shot. Player gun uses full volume (weapons.js); gang shots use `gunshot(.35)` (gangs.js).
- Performance (BUILD 40): merged all static building geometry. `building.js` rewritten: 8 SHARED facade materials (one per palette variant) replace ~150 per-building cloned CanvasTextures — per-building window tiling/offset is baked into geometry UVs (`bakeBoxUVs`); facade boxes are split into side faces (per-variant bucket) and roof top face (bottom face dropped) via `sliceFaces` on non-indexed boxes; parapets/equip/tanks/antennas/tips/doors/awnings each go into material buckets too. New `finalizeBuildings()` (called once in world.js after the block loop) merges each bucket with `mergeGeometries` from three/addons — whole city ≈18 meshes/draw calls instead of ~900, also shrinking the shadow pass. Import map gained the `"three/addons/"` prefix entry (jsdelivr examples/jsm). Trade-off noted: merged meshes skip per-building frustum culling (irrelevant at this draw count). `buildingMats` (used by daynight.js for night windows) now holds just the 8 shared materials. Next perf candidates (not done): InstancedMesh for palms/pines/lamps, ped material sharing + distance-based update skip.
- Visual fixes (BUILD 39): removed the abstract neon strips on buildings (flat MeshBasicMaterial pink/cyan/green/yellow boxes floating .3 off facades — the "meaningless pink/yellow rectangles"); colored door awnings kept. Rural peninsula now has a beach border painted into its canvas ground texture: ~12-unit sand band on the north/south edges and the east tip, with speckled grass→sand transition and a darker wet-sand line at the waterline; the west side already meets the city beach.
- Feature (BUILD 38): on-foot player death now plays a body-fall sequence before the WASTED screen — `getWasted` (player.js) starts a `dying` state (controls locked): over .45s the body rotates to the NPC dead pose (rotation.x → -π/2, face up, y → ground+.35), then a blood puddle spawns (via new `refs.addBloodPuddle`, wired in main.js, avoiding a player↔pedestrians import cycle), and at t=1.5s the original `wastedCut()` (big text + respawn) runs. Vehicle deaths (e.g., plane wreck) keep the instant cut. `getBusted` and repeat `getWasted` calls are guarded during `dying`.
- Removal (BUILD 37): police radio fully removed per user request — deleted `js/police-radio.js`, its import-map entry, the `#police-radio` HTML block, all its CSS (desktop + mobile), the `updatePoliceRadio(dt)` call in main.js, and `reportPoliceCrime` calls in physics.js (addWanted) and weapons.js (shoot). `addWanted` still accepts the third `crime` arg from callers; it is now unused.
- Bug batch (BUILD 36): (1) cop cars merged while chasing — added pairwise separation push (radius 2.9, split half/half) in `updateCops`. (2) On foot you could walk through stopped cars (then get wasted when they moved) — `updateFoot` now pushes the player out of every car using two circles (r 1.3) at ±1.1 along the car axis; planes skipped; runs after `collideStatics`, and is skipped during the enter-car sequence (early return). (3) Explosions now deal damage: new `blastDamage` (radius 5) in weapons.js — wastes the player on foot / dents the driven car, flings peds, kills gang peds, dents + adds 2 bulletHits to nearby cars with chained `explodeCar` via 220ms setTimeout (guarded by arr.indexOf). (4) Deliveries can no longer be completed from the plane or while airborne (`!inPlane && pp.y<3` in updatePickups). (5) Removed the `VEHICLE HIT n/4` message. (6) Driven car now collides with parked `idleCars` (push radius 2.9 + mutual dents above speed 6) in `updateCar`.
- Follow-up: steering wheel was lying nearly flat (legacy rotation.x=-1.15 ≈ 66°); now upright facing the driver (rotation.x=-.35, ~20° column rake), moved closer/reachable at (-.38,.88,.34) with a steering-column cylinder to the dash. Driver arms re-solved with 2-link IK (upper .30 + forearm .27 from shoulders at car-space (-.72/-.04,.74,-.15)) so hands actually rest on the rim: arm x -1.3, forearm x -.78, arm z ±.42 converging to the wheel; spinWheels steer-follow rebased to ±.42 with ±.25 swing. Applied to player pose and NPC `seatDriver`. Badge `BUILD 35`.
- Follow-up: the hollow-shell rebuild left an open gap between the hood's rear edge (z 1.23) and the windshield base (z .85); added a cowl panel there and a parcel-shelf panel for the matching rear slit (rear glass → trunk). Steering now animates: wheel got two spokes and is registered as `userData.steer`; the seated driver (player or NPC) is registered as `userData.driver`; `spinWheels(g,speed,dt,steer)` lerps the wheel spin (-steer*1.8) and both arms' z-rotation so hands follow the wheel. Player passes `input.moveX`, traffic passes its heading delta, police already passed its steer. Driver ref is cleared on exit/busted and set on enter. Badge `BUILD 34`.
- Follow-up: cabin interior was a solid block (single body box ran the full car length), so seats/legs were buried and an open door showed a wall. Rebuilt the body as a SHELL in `car.js`: solid nose (z 1.23..2.18) and tail (-2.19..-1.34), hollow cabin with floor pan (y .09-.15), side sills under the doors, A-pillar strips, and rear quarter panels; seats lowered onto the cabin floor (cushion y .26, backs .50); door now spans sill-to-beltline (.33-.75, pivot y .54); police dark stripe moved from the (now hollow) cabin to hood+tail blocks. Drive pose shows legs again: thighs forward/up (leg rot -2.0), shins near-horizontal (calf +.5), feet on the floor — applied to player (`setDrivePose`) and NPC drivers (`seatDriver`). Badge `BUILD 33`.
- Follow-up: wheels were z-fighting (flickering) against the body because the wheel's outer face (x .70+.13=.83) was exactly coplanar with the body side (1.66/2=.83); moved wheels to x ±.75 so the outer face (.88) sticks out past the body. Car camera brought closer and lower, GTA-style: dist 11.4→7.2, baseH 1.95→1.1 (plane keeps 15.5/1.95). Badge `BUILD 32`.
- Convention change requested by user: STOP per-module `?v=N` import versioning. All JS imports are now clean (no query strings); cache busting is centralized in the `index.html` import map, which maps every local module to its `?v=BUILD` URL (plus the versioned `src` on the main.js script tag). To publish: replace-all `?v=N` in index.html and set the badge to the same number. New modules must be added to the map. CLAUDE.md updated accordingly.
- Current request: rebuild cars to Chevrolet Chevette real-world proportions (4.12m long, 1.57m wide, 1.32m tall, 2.39m wheelbase, hood .90/cabin 2.42/trunk .80, 57cm wheels, 14cm clearance). Used scale ×1.06 so the car matches the existing ~1.86-unit pedestrian (no ped resize needed): car is now 4.37×1.66×1.40 with axles at +1.45/-1.08. Added TWO hinged doors (`userData.doors`, each with an `openSign`); player enters through the nearest door and exits through the driver door. Added NPC drivers seated at the wheel in traffic and police cars via new `seatDriver()` in `js/entities.js` (removed when the car is stolen; `ejectDriver` still spawns the fleeing ped). Traffic now turns corners on a quadratic Bezier (`cornerPos` in `js/traffic.js`, TURN=7m window, slows to 5.5 in corners) instead of snapping 90°. Rescaled collision/dent/seat/exit distances in player/traffic/police/pedestrians, wheel-spin radius .42→.30. Cache-busted `car.js?v=2` (inside entities), `entities.js?v=23` everywhere, `weapons.js?v=19`; badge is now `BUILD 31`. Syntax checks passed for all touched files. Playwright remains skipped per user restriction.
- Current request: place hidden weapons in city plazas and make police confiscate weapons after busted. Replaced the single pickup with one hidden pickup per park/plaza block in `js/weapons.js`, added `confiscateWeapon()` to clear gun/ammo and respawn pickups, wired `getBusted()` through `refs.confiscateWeapon`, cache-busted weapons imports to `weapons.js?v=15`, and bumped badge to `BUILD 15`. Syntax checks passed for weapons/player/main/input. Playwright remains skipped per user restriction.
- Performance (BUILD 50): reduced the dynamic shadow pass for characters and cars. Pedestrians now cast shadow only from their merged body/head mesh instead of arms, forearms, thighs, and calves; cars now cast shadow only from the main body instead of cabin glass, doors, and wheels; seated NPC drivers have `castShadow=false` because their in-car shadows add little visually. Cache-busted all import-map entries and the main module to `?v=50`; badge is now `BUILD 50`. Validation: `node --check assets/models/characters/pedestrian.js`, `node --check assets/models/vehicles/car.js`, and `node --check js/entities.js` passed. Playwright was not run because `progress.md` records the user's prior instruction not to run it unless explicitly requested.

Mobile implementation notes:
- Added normalized `input` state in `js/state.js` and refactored `js/input.js` to export reusable actions for start, interact, pause, radio, shoot, keyboard normalization, and input reset.
- Migrated `js/player.js` movement/car/camera to normalized input, including touch camera look and car camera recenter delay.
- Migrated `js/audio.js` horn and skid sounds to normalized input instead of direct key reads.
- Refactored Diego interaction so dialogue opens through explicit interact action instead of automatic proximity modal.
- Added `js/touch-controls.js` with Pointer Events sticks/buttons, orientation fallback, button labels, and mobile title text.
- Added mobile DOM controls/rotate overlay to `index.html`, mobile HUD/control CSS, safe-area handling, and renderer mobile pixel-ratio/shadow limits.
- Ran `node --check` for touched JS modules; all passed.
- Added explicit `state.weaponHeld` so crosshair/FIRE/weapon targeting depend on the weapon being held, not just owned. Entering a car holsters it; leaving the car re-equips it if owned.
- Fixed touch look-stick orientation: right stick now maps right-to-turn-right and up-to-look-up, and the stick knob keeps its visual center while moving.
- Fixed the left movement stick horizontal orientation by matching the existing movement/vehicle steering convention; pushing right now feeds the correct steering/side direction.
- Added a prominent fixed `FULL` fullscreen button plus a reusable fullscreen toggle action.
- Corrected the right camera stick horizontal orientation after user feedback: left now turns left, right now turns right.
- Fixed the camera-stick sign at the camera application layer: `lookX` positive now increases `cameraRig.yaw`, so dragging the right stick right turns the camera right.
- Reverted camera-stick yaw application to match mouse semantics exactly: right stick produces positive `lookX`, and `updateCamera` subtracts it from `cameraRig.yaw`, same as pointer-lock mouse movement.
- Investigated camera stick chain end-to-end after continued inversion feedback. Final convention: touch/gamepad `input.lookX > 0` means "turn camera right"; `touch-controls.js` maps right-drag to positive `lookX`, and `player.js` applies it with `cameraRig.yaw += input.lookX * dt`. Mouse keeps its separate pointer-lock convention.
- RESOLVED (verified mathematically, do not flip signs again): in this engine yaw INCREASES to the LEFT. Proof: `forward=(sin yaw,0,cos yaw)` with camera behind player means screen-right is -X at yaw=0 and d(forward)/d(yaw)=+X=screen-left; keyboard A (left) sets `moveX=+1`; the working mouse path does `yaw -= movementX*sens`. Therefore "lookX>0 = turn right" requires `cameraRig.yaw -= input.lookX*dt` in `updateCamera`. Commit 2670e7f ("fix") broke it by changing `-=` to `+=`; now restored to `-=` with stick mapping `input.lookX = v.x*YAW_SPEED` unchanged.
- IMPORTANT for testers: git history shows the correct combo (stick `+v.x`, camera `-=`) was already live in db38403, d1ef0e3, and bcb31e8 and was flipped away each time after "still inverted" feedback — the user was almost certainly testing a stale cached build (GitHub Pages deploy lag / mobile browser cache). Always hard-refresh or cache-bust on the phone before judging camera-direction fixes.
- PROOF (real three.js simulation, exact replica of updateCamera steady state, landmark projected to NDC): with `yaw -= lookX*dt` and stick-right (lookX>0), a landmark dead ahead drifts to NDC x = -1.04 (LEFT of screen) => camera turned RIGHT (correct). With the old `yaw += lookX*dt` it drifts to +1.05 => camera turned LEFT — which is exactly the symptom the user keeps reporting, i.e. the signature of the stale/old build. GitHub Pages serves `cache-control: max-age=600` (10-minute browser cache per JS file), so any phone test within 10 min of a push can hit old code.
- Added a build badge to settle which code the phone is running: index.html title screen shows "BUILD 2" and player.js appends " ◆ CAM-R" at module load. The full text "BUILD 2 ◆ CAM-R" on the title screen means BOTH index.html and player.js are fresh; anything else means stale cache. Do not flip camera signs again unless the badge shows fresh AND the camera still turns the wrong way.
- CONFIRMED FIXED by the user with the fresh build: right stick right now turns the camera right. The earlier "still inverted" reports were stale-cache tests. Camera sign convention is settled — `cameraRig.yaw -= input.lookX*dt` with `input.lookX = v.x*YAW_SPEED`. NEVER flip these signs based on a phone test without first checking the BUILD badge on the title screen. The badge can be removed/bumped in future builds as needed.
- Current request: investigate `js/taxi.js` and finish the taxi minigame. Found that `taxi.js` existed but was not integrated: `main.js` did not import/call it and `index.html` did not include it in the import map. Integrated it in BUILD 57, added the missing import-map entry, call to `updateTaxi(dt)`, taxi prompt (`START TAXI SHIFT`), taxi HUD panel, minimap target for the idle taxi/passenger/dropoff, time-limited ride legs, speed-tip payout, shift totals, clean shift cancellation, taxi respawn reset, and `refs.getTaxiState`/`window.render_game_to_text` state hooks. Validation pending below; Playwright remains skipped because this file records the user's earlier instruction not to run Playwright unless explicitly requested.
- Validation for taxi minigame: `node --check js/taxi.js`, `js/main.js`, `js/hud.js`, and `js/state.js` passed; import map parsed successfully with `ConvertFrom-Json`; no `?v=56`/`BUILD 56` remains in `index.html`; local static server started at `http://127.0.0.1:5173/` and returned HTTP 200. Playwright/browser automation was not run due the prior explicit user restriction.
- Follow-up request: improve the taxi mode HUD because the text-heavy panel covered too much of the existing HUD. Replaced the raw multiline `textContent` taxi panel with structured compact DOM rows: title/status chip, objective, optional hint, metric cells, and a time/tip progress bar during rides. CSS now constrains the taxi widget above the minimap on desktop and collapses it to a small top-left chip on mobile, hiding secondary shift totals there. Cache-busted to `BUILD 58`. Validation: `node --check js/taxi.js`, `node --check js/main.js`, and import map parse passed. Browser automation/Playwright still skipped due prior user restriction.
- Follow-up correction after user reported BUILD 58 taxi HUD was worse and covered the previous HUD: simplified it to a much smaller left-side block matching the existing Vice City/neon HUD language. Removed the large internal grid/objective/hint text, leaving only `TAXI`, `PICKUP`/`DROPOFF`, distance, and a tiny timer bar/subline. Desktop block is now 158px wide at left/top; mobile block is 132px wide and hides the subline. Cache-busted to `BUILD 59`. Validation: `node --check js/taxi.js`, `node --check js/main.js`, and import map parse passed. Playwright/browser automation remains skipped due prior restriction.
- Follow-up correction after user said the BUILD 59 taxi HUD was still giant: reduced the taxi HUD to a true small left-side box. It is now 92px wide on desktop, 82px on mobile, with only `TAXI`, `PICK`/`DROP`, distance, and a 2px timer bar during dropoff. Removed subline text entirely. Cache-busted to `BUILD 60`. Validation: `node --check js/taxi.js`, `node --check js/main.js`, and import map parse passed. Playwright/browser automation remains skipped due prior restriction.
- Follow-up correction after user still saw a huge taxi HUD: found the likely root cause was CSS caching (`<link rel="stylesheet" href="css/style.css">` had no cache-bust) plus `#taxihud` still inheriting `.panel`. Changed stylesheet URL to `css/style.css?v=61`, removed `class="panel"` from `#taxihud`, and made the taxi HUD a tiny independent left-side label: 74px desktop / 68px mobile, vertically centered on the left, with only `TAXI`, `PICK`/`DROP`, distance, and a 2px meter on dropoff. Cache-busted to `BUILD 61`. Validation: `node --check js/taxi.js`, `node --check js/main.js`, and import map parse passed. Playwright/browser automation remains skipped due prior restriction.
- Follow-up request: use normal readable HUD font size instead of microscopic text. Kept the taxi box small/left-side and independent from `.panel`, but increased typography to standard HUD sizing: label 9px, action 11px, distance 12px on desktop; mobile label 8px, action 10px, distance 11px. Box is 112px desktop / 96px mobile. Cache-busted CSS and modules to `BUILD 62`. Validation: `node --check js/taxi.js`, `node --check js/main.js`, and import map parse passed. Playwright/browser automation remains skipped due prior restriction.
- Current request: investigate how OVERKILL mode works and finish missing pieces. Found the system already had the totem, prompt, HUD, wanted-level multiplier, capped money rate, and death-based ending wired through `main.js`/`hud.js`/`input.js`/`player.js`, but `overkillMusicOn/Off` were imported and never used, the final "peak" value reported the current multiplier rather than the peak, and `render_game_to_text` did not expose overkill state. Fixed those in `js/overkill.js` and `js/main.js`, removed the unused `radioOn` import, and hid/zeroed the HUD rate while OVERKILL is frozen in cut-scenes/interiors so stale income is not shown. Bumped the title badge to `BUILD 65`. Validation: `node --check js/overkill.js`, `js/overkill-music.js`, `js/main.js`, `js/input.js`, `js/hud.js`, and `js/player.js` passed; `npm run build` passed with only existing Vite dynamic/static import chunk warnings; existing local server at `http://127.0.0.1:5173/` returned HTTP 200 and served `BUILD 65`. Playwright/browser automation remains skipped due prior restriction.
- Follow-up request: make the 3D object used to start OVERKILL be a skull and review the mode. Rebuilt `assets/models/props/overkill-totem.js` as a dark pedestal with a floating bone skull, black sockets/nose/teeth detail, red glowing eyes, and the same animated ring/userData contract consumed by `js/overkill.js`; descriptor label is now `Overkill skull`. Reviewed all OVERKILL references and confirmed the mode is wired through prompt/start/update/death-end/HUD/debug state. Badge bumped to `BUILD 66`. Validation: `node --check assets/models/props/overkill-totem.js`, `js/overkill.js`, `js/main.js`, `js/input.js`, `js/hud.js`, and `js/player.js` passed; `npm run build` passed with only existing Vite chunk warnings; existing local server served `BUILD 66`. Playwright/browser automation remains skipped due prior restriction.
- Follow-up requests: (1) getting busted must also stop/finalize OVERKILL immediately, same as death; `getBusted()` now calls `refs.endOverkill?.()` before the BUSTED cut. (2) car radio must not play during OVERKILL because it mixes with OVERKILL music; `radioOn()`/`radioSwitch()` now no-op and stop radio while OVERKILL is active, entering a car skips radio, and the mobile RAD button hides during OVERKILL. (3) simplified the OVERKILL 3D object to only the floating skull: removed pedestal, ring, and red beacon/facho; `js/overkill.js` now only animates the skull icon. Badge bumped to `BUILD 68`. Validation: `node --check js/radio.js`, `js/player.js`, `js/touch-controls.js`, `js/overkill.js`, and `assets/models/props/overkill-totem.js` passed; `npm run build` passed with only existing Vite chunk warnings; existing local server served `BUILD 68`. Playwright/browser automation remains skipped due prior restriction.
- Current request: add a new prison/presidio place and send the player there after BUSTED, matching the hospital wake-up pattern. Added `assets/models/city/prison.js` with a reserved city block exterior, jail sign, barred facade/watchtower, and hidden internal holding-cell room with guards/inmates/exit arrow; added `js/prison.js` as an `Interior` subclass with `prisonAdmit()` for BUSTED teleports. `world.js` reserves/builds the prison block, `main.js` registers `refs.prisonAdmit` and exposes the active interior in `render_game_to_text`, and `player.getBusted()` now ends OVERKILL, clears police, resets radio/vehicle state, confiscates the weapon, then enters the prison cell instead of spawning on the street. Badge bumped to `BUILD 69`. Validation: `node --check` passed for `assets/models/city/prison.js`, `js/prison.js`, `js/world.js`, `js/main.js`, `js/player.js`, `js/interior.js`, `js/hud.js`, and `js/gangs.js`; `npm run build` passed with only existing Vite dynamic/static import and chunk-size warnings; existing local server served `BUILD 69`. Playwright/browser automation remains skipped due prior restriction.
- Follow-up request: make the prison cells actual closed cells that cannot be entered and change the guard speech bubble to say the crime king paid for release. Updated `assets/models/city/prison.js` so the two inmate cells have concrete side/back walls, locked front bars, visual locks, and AABB collision blockers for side walls, fronts, and backs; moved the BUSTED admit point to the release corridor (`PRISON_RELEASE`) so the player does not spawn inside blocked inmate cells. Updated `js/prison.js` so the guard says "The crime king paid to get you out." Badge bumped to `BUILD 70`. Validation: `node --check assets/models/city/prison.js` and `js/prison.js` passed; `npm run build` passed with only existing Vite dynamic/static import and chunk-size warnings; existing local server served `BUILD 70`. Playwright/browser automation remains skipped due prior restriction.
- Current request: add a new street-race minigame in the GTA 3D style (drive through ordered checkpoints, race NPCs, money reward by finishing position). Added `assets/models/missions/race-gate.js` (checkered start/finish gate, canvas-drawn flag, auto-discovered by the model viewer via the standard descriptor) and `js/race.js` (the whole system). A checkered start gate + orange marker sits at the intersection `(nodeX(2),nodeX(5))`. Drive any car under the gate and stop (`mode==='car'`, dist<5, |speed|<3) to start: 3-2-1 countdown (car frozen via `controlsLocked` + position snap), then a 6-checkpoint route of well-spaced intersections you cross in order, with 3 ghost pace-car opponents (`makeCar`) that drive checkpoint-to-checkpoint. Live HUD panel `#racehud` (right side, mirrors taxi styling) shows POS / CP / TIME; orange radar blip via `refs.raceTarget()`. Finishing pays by place (`[700,350,150,0]` + 1st-place speed bonus). Leaving the car / WASTED / BUSTED aborts the race; re-entry is blocked until you leave the start area. Wired `updateRace(dt)` into `main.js` after `updateTaxi`, added `refs.getRaceState`/`refs.raceTarget`, the race blip in `hud.js` `drawMinimap`, `#racehud` div in `index.html`, and `#racehud`/`.race-*` CSS (desktop + mobile). Validation: `node --check` passed for `js/race.js`, `js/main.js`, `js/hud.js`, and `assets/models/missions/race-gate.js`; `npm run build` passed with only the existing Vite dynamic/static import + chunk-size warnings (race-gate.js shows the same expected model-viewer dynamic-import notice as every other model). Playwright/browser automation remains skipped due prior restriction.
- Follow-up requests on the street-race minigame (7 items): (1) show more checkpoints to orient — `js/race.js` now builds a 3D marker per checkpoint and shows the current one (bright/pulsing) plus the next `CP_AHEAD=2` ahead dimmed; `refs.raceBlips()` returns the current + next checkpoints so the radar draws them too. (2) During a race the radar shows ONLY the race — `js/hud.js` `drawMinimap` now gates gang territories, delivery, taxi and story blips behind `!raceOn` (cop blips kept as a survival hazard, not an objective). (3) Added a bottom-right speedometer (`#speedo` in `index.html`, `hud.js` `updateHUD` shows km/h = `|speed|*5` only in a car; CSS desktop bottom-right with the car name moved up to `bottom:104px`, mobile recentered to bottom-center between the two touch sticks). (4) On accept the player car teleports onto the start line facing checkpoint 0 (`startRace` sets position/heading/speed and `freezePos`); NPCs fan out beside/behind it. (5) Countdown shows 3/2/1 big text with a per-number bip and a start `raceSiren()` (added to `js/audio.js` — three rising whoops); GO blast on release. (6) Created `js/race-music.js`, a race-only ~150 BPM synthwave loop (kick/hat/snare/saw-bass/major arpeggio) started on GO and stopped on finish/abort; the car radio is silenced via `radioOff()` at the start. (7) If all NPCs cross the finish before the player, `loseRace()` ends the race as a loss ("YOU LOST", no prize). Wired `refs.raceBlips`/`refs.getRaceState`; removed the old `refs.raceTarget`. Validation: `node --check` passed for `js/race.js`, `js/race-music.js`, `js/audio.js`, `js/hud.js`, `js/main.js`; `npm run build` passed with only the existing Vite warnings. Playwright/browser automation remains skipped due prior restriction.
- Follow-up tweaks to the street race (4 items): (1) Speedometer now shows ONLY during a race (not in every car) — `hud.js` gates `#speedo` on `refs.getRaceState().phase!=='idle'` && in car. (2) Enemy cars now drive on the streets instead of clipping through buildings — added `buildRoadPath()` in `js/race.js` that turns the checkpoint list into a sequence of street waypoints (L-shaped legs that always run along grid road lines `x=nodeX`/`z=nodeX`, since start and all checkpoints are intersections); `updateRacers` follows these waypoints with a small per-car lane offset, so rivals turn at corners. (3) Only ONE checkpoint shows at a time and they sit closer together — `CP_AHEAD` 2→0 (current marker/blip only) and `CP_SPACING` 55→35 with `CP_COUNT` 6→8. (4) Camera is pre-aligned behind the car at the start — `startRace` sets `cameraRig.yaw=h0` (imported from player.js) so the chase cam immediately faces down the route. Validation: `node --check js/race.js` and `js/hud.js` passed; `npm run build` passed with only the existing Vite warnings. Playwright/browser automation remains skipped due prior restriction.
- More street-race tweaks (6 items): (1) Shorter race — `CP_COUNT` 8→6. (2) Rivals never cut corners — they aim EXACTLY at each street waypoint (removed the lateral lane-aim) and only advance when within `NPC_REACH=3.5`, so they genuinely pass through every point. (3) Rivals slower — speed `17+i*2.4` → `11+i*1.4` (≈11/12.4/13.8) with staggered grid spawn, giving the player a real chance. (4) Gangs are hidden during a race and restored after — `gangs.js` gains `setGangsHidden()` (toggles `gangPeds` visibility + early-returns `updateGangs`), exposed as `refs.setGangsHidden` from `main.js`; `race.js` calls it true on start, false on finish. (5) Camera is now actually locked facing forward — during the countdown `race.js` forces `cur.heading`/`cur.g.rotation`/`cameraRig.yaw` to `freezeHeading` every frame (not just once), so the chase cam looks down the route from the GO. (6) Endless races — `start` is now mutable with a captured `gate`; `relocateStart()` moves the gate+marker to a new far intersection inside `finishRace()`, so completing/losing/abandoning a race spawns the next one elsewhere, forever. Validation: `node --check js/race.js`, `js/gangs.js`, `js/main.js` passed; `npm run build` passed with only the existing Vite warnings. Playwright/browser automation remains skipped due prior restriction.
- More street-race fixes (5 items): (1) Start arch now faces the first checkpoint — added `prepareRace()` (called at module load and inside `relocateStart()`) that pre-generates the route and sets `gate.rotation.y` toward `route[0]`; the player teleports to the gate center facing the same heading, so they spawn INSIDE the arch pointing down the course (no longer beside a post). (2) Race now starts only on interaction — removed the proximity auto-start; added `refs.raceNear()`/`refs.startRaceInteract()`, wired `performInteract` (car branch) to start the race with priority over exiting, and `hud.js` shows a "START THE RACE" E-prompt at the gate. (3) No more U-turn checkpoints — `genRoute` rewritten as `tryRoute()` (+16-attempt retry) that rejects any leg whose direction dot-product with the previous leg is below `TURN_MIN=-0.1`, so the course only ever goes forward/left/right, never reversing. (4) The start gate is removed once the race begins — `gate.visible=false` in `startRace`, restored in `finishRace`. (5) The final checkpoint shows a distinct finish line — new `assets/models/missions/race-finish.js` (two poles + checkered tape at car height, a canvas "FINISH" banner, a small spectator crowd, and a waving checkered flag); `buildCpMarkers` places it at the last route point oriented toward the incoming car, and `updateCpMarkers` shows it only when it's the current target and waves its flag. Validation: `node --check js/race.js`, `js/input.js`, `js/hud.js`, `assets/models/missions/race-finish.js` passed; `npm run build` passed with only the existing Vite warnings (incl. the standard model-viewer dynamic-import notice for race-finish.js). Playwright/browser automation remains skipped due prior restriction.
- Street-race fix: the start and finish points can never be on gang territory. `race.js` now imports `gangs` and adds `inGangTerritory(x,z)` (checks each gang center against its current radius + `GANG_MARGIN=8`; territories only shrink, so the current radius is sufficient). `relocateStart()` rejects candidate intersections inside any territory (tries bumped to 60), and `tryRoute()` rejects the final checkpoint if it lands in a territory. Validation: `node --check js/race.js` passed; `npm run build` passed with only the existing Vite warnings. Playwright/browser automation remains skipped due prior restriction.
- Polish pass / review of the street race. Audited the full flow and confirmed: the race always terminates (rivals follow a finite road path with no collisions, so they can never get stuck — once all finish it triggers loseRace), every exit path (complete/lose/abort) runs finishRace which clears racers+markers, restores gangs/radio/music/camera/controlsLocked and relocates the gate, the gang-territory rule holds even at module load (gangs imported directly, full radius at load), and pausing freezes the countdown. Cleanup: removed the now-dead `reentryBlock` flag (always false since the start became interaction-only) from its declaration, `raceBlips`, `raceNear`, and `finishRace`; behavior is identical, intent is clearer. Validation: `node --check js/race.js` passed, no remaining `reentryBlock` refs, `npm run build` passed with only the existing Vite warnings. Playwright/browser automation remains skipped due prior restriction.
- Two requests: (1) Camera should sit behind the car on entry and gently auto-follow while driving so the player isn't constantly re-aiming it. In `player.js` `updateCamera`, car mode now auto-recenters behind the car (lerp toward `cur.heading` at rate `dt*2.0`) once `cameraRig.touchLookIdle>.45` — i.e. shortly after the player stops giving look input — and this works even under mouse pointer-lock (foot mode keeps the old "recenter only when no pointer-lock/touch" rule). `completeEnter` snaps `cameraRig.yaw=cur.heading` (and primes `touchLookIdle`) so entering a car puts the camera behind it immediately. `input.js` mousemove now resets `cameraRig.touchLookIdle=0` so active mouse-look defers the auto-follow (no fighting). (2) Rival race cars were empty — `spawnRacers` in `race.js` now calls `seatDriver(g,pick(shirtColors))` (from entities.js) so each opponent has a driver at the wheel; existing `spinWheels(r.g,...)` already animates the driver's arms, and `clearRacers` removes the driver with the car. Validation: `node --check js/player.js`, `js/input.js`, `js/race.js` passed; `npm run build` passed with only the existing Vite warnings. Playwright/browser automation remains skipped due prior restriction.
- Current request: add a rideable motorcycle, GTA 3D style, done well. Added `assets/models/vehicles/motorcycle.js` (auto-discovered by the model viewer via the standard `{category,label,build,variants}` descriptor) — a cruiser/street bike from primitives: two wheels (chrome disc + tire), engine block, fuel tank, seat, rear cowl/fender, twin chrome exhausts + footpegs + kickstand, and a STEERING fork group holding the front wheel, fork legs, front fender, handlebar with grips/mirrors and the headlight. Shares the car's optimization spirit: module-level shared materials + a `paintCache` per color (3 color variants). The bike exposes the same spin contract as the car so `spinWheels` works unchanged — `userData.wheels=[frontWheel,rearWheel]` (rolled via `rotation.x`) and `userData.front=[fork]` (steered via `rotation.y`); it deliberately has NO `userData.dentable` (bikes don't dent), no steering wheel and no doors. Gameplay treats it via a new `bike:true` flag on the vehicle object (mirrors the existing `plane` flag): `makeMotorcycle` re-exported through `entities.js`; `player.js` spawns two parked bikes (`STREET ROCKET` next to the start car, `NEON BLADE` at intersection nodeX(5)/nodeX(5)) into `idleCars` like the playerCar (free to take, no wanted). Mount/dismount reuse the car's enter/exit sequence but with no door (door resolves to null → direct mount like the plane). New `setRidePose()` poses the rider visibly ON TOP (hands forward/down on the bars, legs splayed/bent to the pegs); `completeEnter` seats the rider at local y=`BIKE_SEAT=.3`, does NOT set `userData.driver` (so spinWheels' steering-wheel arm IK doesn't yank the rider's arms) and skips the car radio. `updateCar` gained `bike` tuning: faster (MAX 42 vs 32), stronger accel (22 vs 16), sharper steering (2.4 vs 2.0, ref speed 9), barely any reverse (-8), and a pronounced LEAN INTO the turn via `rotation.z` (±.5 rad vs the car's ±.06 roll) — the rider, being a child, leans with it. Water-sink, static/idle-car collision, terrain follow and `spinWheels` are all shared with the car path. HUD: `getInteractAction` shows `RIDE THE BIKE` / `GET OFF THE BIKE` for bikes; `audio.js` engine note is higher-pitched for bikes (base 92 vs 52). Cosmetic badge bumped to `BUILD 71` and `player.js` appends ` ◆ BIKE`. Validation: `node --check` passed for `assets/models/vehicles/motorcycle.js`, `js/player.js`, `js/entities.js`, `js/audio.js`, `js/hud.js`; `npm run build` passed with only the existing Vite dynamic/static import + chunk-size warnings. Playwright/browser automation remains skipped due prior restriction.
- Review pass on the motorcycle ("está no nível de moto do GTA?"). (1) Lean direction re-verified against the engine proof in this log (line 69: +X = screen-left, yaw increases to the left): `st>0` is a left turn and `leanT=-st·k` tips the bike top toward +X = INTO the turn — correct. (2) Rider pose was geometrically wrong: using the real ped limb dims from `pedestrian.js`, the palm reach from the shoulder is ~.62 but the handlebar sat at world y≈1.0/z≈.5, leaving the hands ~13 cm short/high (floating off the bars). Raised + recessed the handlebar to a cruiser position (fork-local y .4→.55, z -.06→-.14 → world ≈(±.27,1.15,.42)) and re-solved the arm angle (`rightArm.x` -1.0→-.86, forearm -.25→-.15, z-spread .18→.1) so the palms land on the grips. (3) Night parity: cars have a `beamMat` headlight cone toggled by `daynight.js`; the bike had none, so it rode dark at night. Imported the shared `beamMat` from `car.js` and added a smaller (3.2×5.0) ground cone in front — now lights up at night with the same global control. Deliberately OUT of scope (noted, not defects): NPC bikers in traffic (would need traffic.js bike paths) and a ragdoll wipeout on hard crashes (the car also just bounces). Validation: `node --check assets/models/vehicles/motorcycle.js` and `js/player.js` passed; `npm run build` passed with only the existing Vite warnings. Playwright/browser automation remains skipped due prior restriction.
- Two user fixes on the motorcycle: (1) the handlebar looked floating — nothing connected the steering head to the raised cruiser bar (the fork legs only went DOWN to the wheel). Added a top yoke (`yokeG` bridging the two legs), short upper fork stanchions above the yoke, and an angled riser tube (`riserG`, rotation.x=-.3) from the yoke up to the bar center, so the bar is now physically attached. (2) Cornering lean wasn't noticeable — it required near-MAX speed. Re-tuned the bike lean in `updateCar` to reach full ~.42 rad (~24°) by speed 18 (`clamp(cur.speed/18,0,1)*.42`) instead of `clamp(speed/42)*.5`, with a gentler lerp (6·dt) so it eases in/out of the lean like a real bike; still `-st` = into the turn, clamped at 0 so reverse doesn't tilt. Validation: `node --check assets/models/vehicles/motorcycle.js` and `js/player.js` passed; `npm run build` passed with only the existing Vite warnings. Playwright/browser automation remains skipped due prior restriction.
- Current request: GTA-style buyable rural safehouse with a fridge health item, interior, and a persistent garage. New model `assets/models/rural/ranch-house.js` (auto-discovered by the viewer via the standard descriptor) builds, at (245,-95) in the rural peninsula: a cream house body + hip roof + porch/columns/windows with the front door on the north face; an adjacent open-front GARAGE (three solid walls, north opening, concrete slab) to the west at (234,-95); a FOR SALE / SOLD canvas sign pair and a bouncing door arrow in the yard; and the INTERIOR ~600m off-map at (-800,80) (z chosen free between nightclub -22 and hospital 180) — a 16×12 shell with wood floor, kitchen counter + fridge + a floating food plate (heal item), living room (sofa facing a TV, coffee table, rug), a bedroom bed, a dining table, a warm point light, and the west exit door+arrow. Exterior house + 3 garage walls + 4 interior walls pushed to `solids`. New system `js/property.js` extends the `Interior` base (so door/teleport/camera/world-bound/emergency-exit are inherited): `HouseInterior.near()` returns null until owned (door only works after purchase); `update()` toggles the FOR SALE/SOLD signs and the facade arrow by ownership; `updateFx()` bobs the food. Purchase: `$5000`, triggered by `houseBuy()` when on foot within 2.6 of the sign (`houseBuyState()` drives the HUD `BUY` prompt); ownership persists in `localStorage` key `tinygta_property`. Fridge heal: `houseEat()` restores health to 100 when on foot inside near the food (`houseEatState()` → HUD `EAT`). Garage: `houseGaragePark()` (HUD `PARK` when in a stopped car within 3.4 of the pad) saves the current vehicle's `{type,color,name}` to localStorage and exits the player (`refs.exitCar`); on boot `initProperty()` respawns the saved vehicle parked in the garage as a normal idleCar (so it's taken/driven like any car — retrieval needs no special action). To support color round-trips, `buildCar`/`buildMotorcycle` now stash `g.userData.color`. Wiring: `world.js` calls `addRanchHouse(solids)` in the rural section; `main.js` imports the property module, exposes `refs.exitCar/houseBuyState/houseEatState/houseGarageState/getHouseState`, calls `initProperty()` after refs, and adds `house` to `render_game_to_text`; `input.js` `performInteract` calls eat/buy (foot, before gym/story/enterCar) and garage-park (car, before exit); `hud.js` `getInteractAction` surfaces the eat/buy (foot) and garage (car) prompts. Gangs are kept away via the Interior `exterior:{x:245,z:-95,r:16}` config. Validation: `node --check` passed on all touched files; `npm run build` passed with only the existing Vite warnings. Playwright/browser automation remains skipped due prior restriction.
- Follow-up on the rural safehouse (4 requests): (1) House visual reworked into a traditional countryside farmhouse in `ranch-house.js` — stone foundation plinth, light clapboard body, a real two-slope GABLE roof (two tilted roof panes + triangular `ShapeGeometry` gable ends + dark ridge board) replacing the old 4-sided cone, a brick chimney, white corner trim + eave fascia, a framed front door with two concrete steps, windows upgraded to white frame + muntin cross + green shutters (front pair + three side windows), and a covered front porch (wood deck, two white columns, low railings, sloped porch roof). The model-viewer preview build() and the house collision height (h 5→5.3) were updated to match. (2) FOR SALE/SOLD sign fixed — the single center post used to run through the middle of the board; `makeSign` now uses two side posts + a thin frame + a top rail with the board mounted slightly proud, so nothing crosses the text. (3) Price set to $1 for testing — new exported `HOUSE_PRICE=1` in the model drives both the sign canvas ($ + HOUSE_PRICE) and `property.js` (`PRICE=HOUSE_PRICE`), keeping them in sync. (4) Green wall request — new model `assets/models/rural/hedge.js` (`{category,label,build}` + `addHedge(cx,cz,length,solids,opts)` helper) builds a long foliage hedge with an irregular trimmed top, merged into ~2 meshes via mergeGeometries, with a solid collision AABB; `addRanchHouse` plants one north–south at x=214, z -140..-30, height 8 — between the house and the city, screening the city buildings from the house's view. It stops well short of the dirt road (z~0) so the drive-in route to the property stays open. Validation: `node --check` passed on ranch-house.js, hedge.js, property.js; `npm run build` passed with only the existing Vite dynamic/static-import warnings (now also listing hedge.js/ranch-house.js, expected for model-viewer auto-discovery). Playwright/browser automation remains skipped due prior restriction.
- User follow-up on safehouse: hedge was too large, sign text was invisible, and the sign should disappear after purchase. Changed `addRanchHouse` to plant a short hedge roughly house-width (`W+2`, height 4.2, thickness 1.8) instead of the 110m screen wall. Rebuilt `makeSign` with a hollow frame and two textured board faces so the FOR SALE + `$HOUSE_PRICE` canvas is not covered. `property.js` now hides any sold sign and keeps the sale sign visible only while unowned, so after purchase the yard sign disappears. Validation: `node --check` passed for `ranch-house.js`, `hedge.js`, `property.js`, `main.js`; `npm run build` passed with only the existing Vite dynamic/static import and chunk-size warnings. Browser automation remains skipped per prior user restriction.
- Safehouse polish/review follow-up: fixed leaving the house sideways by adding configurable `spawnOutHeading` to `Interior` (default remains `-Math.PI/2` for existing interiors) and setting the ranch house to `Math.PI`, so the player exits the north-facing door looking out toward the porch. Exterior polish: added siding strips, attic round window, hollow door/window frames, knob, porch lantern, flower boxes, fuller porch rails, bench, walkway, and improved model-viewer preview. Interior polish: added ceiling, baseboards, upper kitchen cabinets, sink, stove/burners, couch pillows, bed headboard, floor lamp, nightstand, dining chairs, wall art, and TV color animation. Bug review fix: garage now tracks the currently saved spawned vehicle and removes/replaces the previous one when another vehicle is stored, avoiding two saved vehicles stacked in the garage. User also noticed the small front porch roof was inverted; changed porch roof `rotation.x` from `.26` to `-.26` in both the world model and model-viewer preview. Badge is now `BUILD 72`. Validation: `node --check` passed for `ranch-house.js`, `hedge.js`, `property.js`, `interior.js`, and `main.js`; `npm run build` passed with only existing Vite model-viewer dynamic/static import and chunk-size warnings. Browser automation remains skipped per prior user restriction.
- Model-viewer visual inspection follow-up: added explicit model-viewer variants for `Ranch house - exterior` and `Ranch house - interior`, plus optional per-entry `zoom` support in `js/model-viewer.js` so wide/low objects can be inspected without looking tiny. Captured and inspected `output/model-preview/ranch-exterior-front-zoom.png`, `ranch-interior-cutaway-zoom.png`, and `ranch-exterior-porch-side.png`. Findings: exterior is now readable in the preview, the small porch roof slopes outward correctly, and the interior cutaway shows kitchen/living/bed/dining details without obvious clipping. Console errors seen during preview were unrelated existing localhost issues (leaderboard CORS + missing favicon), not model errors. Validation: `node --check` passed for `ranch-house.js`, `model-viewer.js`, `property.js`, and `interior.js`; `npm run build` passed with only existing Vite dynamic/static import and chunk-size warnings.
- Safehouse relocation follow-up: removed the green wall/hedge entirely (`assets/models/rural/hedge.js` deleted, no `addHedge` import/call left), so the model-viewer rural list now contains only Barn, Hay, Ranch house exterior/interior, and Summit flag. Moved the ranch house from the earlier mid-rural spot to the far rural peninsula beside the mountain: house center `(420,-80)`, front door `(420,-85.5)`, spawn/buy sign `(420,-88)`, garage pad `(409,-80)`. Checked against `constants.js`: all points are inside `RURAL_X0=183..RURAL_X1=443`, within `RURAL_HALF=120`, on `groundHeight=0`, and outside the mountain radius (`MOUNT_X=379`, `MOUNT_R=62`; closest checked house corner dist≈91.9), so it sits on flat land beside the mountain rather than embedded in it. Added a pine-tree exclusion zone around the house/garage so random rural trees cannot spawn clipping through the new safehouse area. Badge bumped to `BUILD 73`. Visual check captured `output/model-preview/ranch-exterior-final.png` and `ranch-interior-final.png`; exterior/interior still render, the front porch roof remains visible/connected, and the hedge option is absent. Validation: `node --check` passed for `ranch-house.js`, `property.js`, `model-viewer.js`, `interior.js`, and `world.js`; `npm run build` passed with only the existing Vite dynamic/static import and chunk-size warnings.
- Safehouse camera-clip bugfix: matched the hospital/gym/prison facade contract for the ranch. `ranchFx` now exposes `facade` + `footprint`; all attached exterior details that would otherwise float when the camera starts inside the house footprint after exiting (siding strips, gable pieces, roof panes/ridge/chimney, corners/fascia, door/frame/knob/lantern/steps, windows/shutters/flower boxes, porch deck/columns/rails/bench/porch roof, and the facade arrow) are parented under `ranchFx.facade`. `Interior.updateFacade()` already hides that group whenever the camera is inside `fx.footprint`, so the house now follows the same occlusion rule as the other interiors. Updated `js/interior.js` comments to make `fx.facade + fx.footprint` an explicit implementation obligation for any new interior with separate exterior door/facade details. Badge bumped to `BUILD 74`. Validation: `node --check` passed for `ranch-house.js`, `property.js`, `interior.js`, `world.js`, and `model-viewer.js`; `npm run build` passed with only the existing Vite dynamic/static import and chunk-size warnings.
- Safehouse TV interaction: added `js/house-tv.js` and a `#house-tv` overlay/iframe in `index.html` + CSS. When the player is inside the house and close to the living-room TV (`TV` exported from `ranch-house.js`), the HUD action becomes `TV / USE TV`; pressing `E/F` opens an iframe to `https://andredarcie.github.io/andre-os/`, exits pointer-lock, locks all game input, freezes the sim by returning early in `main.step()`, and forces the Three.js camera into a first-person sofa view focused on the TV. The iframe takes most of the screen and is the only interactive pointer/touch area; mobile gets a dedicated `EXIT` button in the TV overlay, and desktop exits with `E/F` while the parent page has focus (the iframe is `tabindex=-1` and `house-tv.js` tries to refocus the parent on blur so E/F remain available while pointer/touch still work inside the iframe). While active, global shortcuts like model-viewer/pause/radio are ignored, and `render_game_to_text` exposes `houseTv:{active,url,near}`. Also corrected the 3D TV orientation in the house/interior preview so the screen faces the sofa. Badge bumped to `BUILD 75`. Validation: `node --check` passed for `house-tv.js`, `input.js`, `hud.js`, `main.js`, `touch-controls.js`, `state.js`, and `ranch-house.js`; `npm run build` passed with only the existing Vite dynamic/static import and chunk-size warnings.
- Safehouse TV follow-up: the player mesh was still visible on the right side while the TV camera was active. `house-tv.js` now stores `player.g.visible`, hides the player immediately on TV open and during each active TV frame, then restores the previous visibility on exit. Badge bumped to `BUILD 76`. Validation: `node --check js/house-tv.js` passed; `npm run build` passed with only the existing Vite dynamic/static import and chunk-size warnings.
- Safehouse TV preview follow-up: replaced the plain blue in-world TV screen with a static high-contrast `CanvasTexture` preview of the `andre-os` page (large ANDRE OS title, URL, app cards, dock icons, cursor) so the player can see a page-like preview on the TV before entering TV mode and from farther away in the room. The model-viewer interior preview uses the same texture. `property.js` now keeps textured TV screens untinted while preserving the old color animation fallback for any non-textured screen. Badge bumped to `BUILD 77`. Validation: `node --check` passed for `ranch-house.js` and `property.js`; `npm run build` passed with only the existing Vite dynamic/static import and chunk-size warnings.
- Speedboat (Vice City style): new `assets/models/vehicles/boat.js` — an open-cockpit speedboat with a V-hull built from an `ExtrudeGeometry` outline (pointed bow, flat stern), cream deck with a cockpit cutout, slanted windshield, steering console + chrome wheel, two seats, stern engine cowl + chrome U-rail, and a bow nav light. Follows the standard model descriptor (pure `build(opts)`, color variants coral/aqua/magenta, `makeBoat(color)` compat wrapper that adds to the scene), so it auto-appears in the model viewer. `entities.js` re-exports `makeBoat`. Gameplay (`js/player.js`): `spawnBoat` anchors one `boat:true` craft in the sea south of the beach (`SEA BLASTER` at `x=24, z=WATER+12`); it's a free vehicle like the bike. Boarding is direct (no door, like the bike): `completeEnter` seats the player at the console with `setDrivePose`. `updateBoat(dt)` is the INVERSE of the car: it planes across water and BEACHES on sand/land (heavy drag when `inWater` is false), never sinks, steers only while moving, lifts the bow when planing, banks into turns, and bobs on the waterline at `SEA_Y=-.32`; clamped to `SWIM_BOUND`. `updateCar` early-returns to `updateBoat` for boats. HUD shows `BOAT / RIDE THE BOAT` and `GET OFF THE BOAT`. Badge bumped to `BUILD 78`. Validation: `node --check` passed for `boat.js`, `player.js`, `hud.js`, `entities.js`; `npm run build` passed with only the existing Vite dynamic/static import and chunk-size warnings.
- Speedboat model + seating overhaul: rebuilt `assets/models/vehicles/boat.js` with much more detail and corrected captain seating. New parts: two-tone V-hull (dark bottom scaled-in + painted topsides + waterline boot stripe via three stacked `ExtrudeGeometry` passes of one outline), cream side/fore/aft decks with a low teak cockpit sole, chrome rubrails, a central helm console with a raked dash + nav screen + throttle lever, a chrome destroyer wheel whose inner rim rotates with steering (`userData.steer`, lerped from `input.moveX` in `updateBoat`), a wraparound 3-pane raked windshield with chrome posts/frame, a pedestal captain bucket seat behind the wheel, a stern engine hatch + rear bench + transom with exhaust ports, a swim platform + chrome boarding ladder, plus bow rail/cleats and red/green bow nav lights and a white stern mast light. The seating was calibrated against the pedestrian rig (origin at feet, hip .55, shoulder 1.26, head 1.62): added `setBoatPose()` in `player.js` (thighs ~horizontal forward `legRot=-1.3`, shins down `calfRot=+1.4`, arms forward-down `armRot=-1.15`/`forearm=-.55`) and computed the leg/arm forward kinematics so the player offset `(0,-.08,-.15)` lands the feet on the cockpit sole (~y.09) and the hands on the helm rim, with the wheel placed at `(0,1.13,.54)` to meet the computed grip point. `completeEnter` boat branch now uses `setBoatPose` instead of the car's `setDrivePose`. Badge bumped to `BUILD 79`. Validation: `node --check` passed for `boat.js` and `player.js`; `npm run build` passed with only the existing Vite dynamic/static import and chunk-size warnings. NOTE: seated pose was tuned by kinematic math, not yet visually confirmed in-browser (per the standing no-Playwright-unless-asked instruction).
- Speedboat hull gap bugfix: the hull and the deck/console/seats weren't connecting — a ~0.5m gap. Root cause: `ExtrudeGeometry` extrudes along +Z and, after `rotateX(Math.PI/2)`, that +Z maps to −Y, so the hull extrudes DOWNWARD; the old code placed the hull at y=0 (occupying [−depth,0], all below the waterline) while decks/console/seats sat at y≈0.5+, leaving everything floating above the hull. Rebuilt the hull in `boat.js` so each extruded piece is POSITIONED BY ITS TOP (documented at the top of the file): a solid lower hull spanning [−.45,.13] plus an upper costado ANEL built from the hull outline WITH a cockpit `hole` (so the cockpit is genuinely open) spanning [.10,.55] up to the deck — the two overlap at [.10,.13] with no gap. The ring uses a `DoubleSide` clone of the paint so the cockpit's inner walls render. Cream deck boxes (fore/aft/side) sit on top of the ring at y=.57, leaving the cockpit open; teak sole at y=.12. Re-calibrated the captain offset to `(0,-.05,-.15)` so feet land on the ~.12 sole and hands meet the helm at `(0,1.14,.54)`. Badge bumped to `BUILD 80`. Validation: `node --check` passed for `boat.js` and `player.js`; `npm run build` passed with only the existing Vite warnings. Still tuned by math, not yet visually confirmed in-browser.
- Speedboat wake/spray effect: new `assets/models/effects/boat-wake.js` — a flat foam patch (canvas foam texture, geometry pre-laid in XZ via `rotateX(-PI/2)`, additive-free transparent `MeshBasicMaterial`, opacity 0 by default). `player.js` runs a recycled particle pool (`wake[]` active + `wakePool[]` freelist so no per-frame allocation): `spawnPuff` places foam on the sea surface (`SEA_Y+.02`) with a horizontal drift velocity, `updateWake` drifts/decays the velocity, grows the patch, and pops opacity in then fades to zero before recycling. In `updateBoat`, when on water and `|speed|>3`, it emits on a speed-scaled interval: two side-spray puffs born at the bow quarters and thrown OUTWARD (±right * (1.5+speed*.06)) plus a bit backward, and one wide near-stationary stern-trail puff (the boat moves away from it) — giving the sense of water pushed out the sides and a foamy wake behind. `updateFoot` also drains the pool so leftover foam fades after you hop off. Badge bumped to `BUILD 81`. Validation: `node --check` passed for `boat-wake.js` and `player.js`; `npm run build` passed with only the existing Vite warnings.
- Speedboat pointed foredeck: the cream bow deck was a rectangular `BoxGeometry(1.7,.06,1.0)`, so it read as a square block sticking out at the bow while the hull tapered to a point. Replaced it with a `foredeckShape()` panel (new `extrudeHull(foredeckShape(),0.06)` → `foredeckGeo`) whose outline follows the hull's front contour: from the cockpit front edge out to the gunwale and up to a single bow tip at z=2.36, with a shallow rear V matching the cockpit opening point. Because `foredeckGeo` already carries the boat X/Z from the shape, the mesh just rises to deck level (`position.y=.60`) with no z offset/rotation. Now the cream deck tapers to the bow like the rest of the hull. Badge bumped to `BUILD 82`. Validation: `node --check` passed for `boat.js`; `npm run build` passed with only the existing Vite warnings.
- Weapon base class + all 13 GTA III weapons (request: "crie uma classe base para armas... crie exatamente essas que faltam"). Before there were only the pistol and the rural-rampage bazooka, hardcoded in `js/weapons.js`. Introduced an OOP, data-driven arsenal: `js/weapon-types.js` defines the base `Weapon` class (id/name/slot/model/hold/fireRate/automatic/ammo/recoil + `tryFire(api)` template method that checks cadence/ammo, poses, calls subclass `onFire`, consumes ammo and applies recoil) and the category subclasses `MeleeWeapon`, `FirearmWeapon`, `RocketWeapon`, `FlamethrowerWeapon`, `ThrownWeapon`, `DetonatorWeapon`. The classes are pure logic — all game effects (aim ray, bullet/missile/projectile/flame spawns, hit detection, sounds) are injected via an `api` object built in `weapons.js`, so the classes never import the gameplay module (no import cycle). `js/weapon-catalog.js` instantiates the 13 weapons (Fist, Baseball Bat, Pistol, Uzi, Shotgun, AK47, M16, Sniper Rifle, Grenade, Molotov, Rocket Launcher=the bazuca model, Flamethrower, Detonator) with their stats and 3D-model factories, exposes `WEAPONS`/`ARSENAL`/`FIST`/`bySlot`. Created 10 new model files in `assets/models/weapons/` (baseball-bat, uzi, shotgun, ak47, m16, sniper-rifle, flamethrower, molotov, grenade, detonator) plus 2 effects (`effects/fire.js` molotov fire pool, `effects/flame-jet.js` flamethrower cone), each following the standard descriptor so they auto-appear in the model viewer. `weapons.js` was refactored: a generic `heldHolder` swaps the held model on `equip()`; `shootWeapon()` delegates to `curWeapon.tryFire(api)` (rampage still uses the special missile path); new behaviors `fireOneBullet` (per-weapon range/speed/damage/spread + multi-pellet shotgun), `meleeAttack`, `flameAttack` (short cone), `throwProjectile` (grenade with fuse + bounce, molotov bursting into a damaging fire pool), `detonatorAction` (plant then detonate), plus update loops for thrown/flame-jets/fire-pools and held-weapon flame animation; `grantWeapon` now hands the whole arsenal, `confiscateWeapon` resets to fists. Inventory switching: number keys 1-0 (0=molotov slot 10), mouse wheel, `Q`, and a new mobile `WPN` button; desktop mousedown/up drives held auto-fire via `input.shootHeld`. HUD shows the weapon name + ammo (`∞` for fists/flamethrower/detonator) via `refs.getWeaponHud`; the FIRE button now appears on foot even unarmed (punch). Badge bumped to `BUILD 83`. Validation: `node --check` passed for all 12 new model/effect files and `weapon-types.js`, `weapon-catalog.js`, `weapons.js`, `input.js`, `main.js`, `hud.js`, `touch-controls.js`; `npm run build` passed with only the existing Vite dynamic/static-import (model-viewer glob) and chunk-size warnings. Playwright/browser automation not run per the standing instruction.
- Arsenal follow-up to match the 13 GTA III names exactly (request: rename the bazooka, remove the gang-gun and let gangs use real weapons, make the player-gun the standard GTA III pistol). (1) Renamed the bazooka → Rocket Launcher: model file `assets/models/weapons/bazooka.js` → `rocket-launcher.js`, factory `makeBazookaModel` → `makeRocketLauncherModel`, descriptor label `Bazooka` → `Rocket Launcher`; updated all importers (`weapons.js`, `weapon-catalog.js`, `police.js`), renamed the rural-rampage internals (`heldRocket`/`rocketPickup`/`ROCKET_X/Z`/`rocketRespawnAt`), the rampage HUD message ("…WITH THE ROCKET LAUNCHER"), the police officer `o.rocket` flag, and the bazuca comments → lança-foguetes. (2) Deleted `assets/models/weapons/gang-gun.js`; `entities.js attachHandGun(ped, kind='pistol')` now hangs a REAL arsenal model in the NPC's right hand — the scaled-down player Pistol or Uzi (`HAND_WEAPONS` table, `castShadow=false` on all gun meshes to keep the many-NPC render cheap). Gangs (`gangs.js`) now carry a random pistol/uzi; police keep the pistol; 5-star rocket cops keep the rocket launcher. (3) Rebuilt `assets/models/weapons/player-gun.js` from the Glock look into the GTA III standard pistol (Colt M1911 style: steel-grey slide with barrel bushing, EXTERNAL spur hammer at the rear, rounded trigger guard, thumb safety + slide stop, beavertail, dark grip panels + serrated mainspring housing) while keeping the same `makeGunModel({pickup})` interface, scale/bore axis and `userData.muzzlePoint` so all hold offsets and the field pickup still line up. Badge bumped to `BUILD 84`. Validation: `node --check` passed for `player-gun.js`, `rocket-launcher.js`, `weapon-catalog.js`, `weapons.js`, `police.js`, `entities.js`, `gangs.js`, `weapon-types.js`, `player.js`; `npm run build` passed clean (no unresolved imports; only the pre-existing model-viewer glob + chunk-size warnings); repo-wide grep confirms no `bazooka`/`gang-gun`/`makeGangGun`/`makeBazookaModel` references remain. Playwright/browser automation not run per the standing instruction.
- Gun shop "AMMU-NATION" with buyable weapons (request: a weapon store following the current building pattern, interior with 3 counters holding all weapons except Fist = the 12 arsenal weapons, approach + interact to buy at a price, with the weapon name labeled above each item on the counter; player-gun must display as "PISTOL"). Built exactly on the existing interior pattern (gym/hospital/prison): new model `assets/models/city/gun-shop.js` reserves city block (1,5) (`GUNSHOP_I/J`, center (-110,66)) with an exterior shop (yellow band, marquee, AMMU-NATION canvas sign, door + bouncing arrow, facade group + footprint + solid) and a separate interior room ~600m off-map (group `visible=false`, toggled on enter) at (-800,-380), 30×22, with 3 island counters at z=-374/-380/-386. The 12 weapons are auto-pulled from the catalog (`ARSENAL`, so the showcase grows with the arsenal): each weapon model is scaled to ~0.95 and centered in a spin pivot resting on the counter, with a canvas name label floating above (font auto-shrinks for long names like "MOLOTOV COCKTAIL"). New gameplay module `js/gun-shop.js` adds `GunShopInterior extends Interior` (spins the displays, billboards the labels toward the camera each frame, idle-animates a shopkeeper ped), plus `gunShopState()` (HUD shows `BUY <NAME> $<price>` / `NEED $X` / `OWNED` within 2.8 of a counter weapon) and `gunShopBuy()` (charges money, grants the weapon). `weapons.js` gained `buyWeapon(id)` (adds ONE weapon to `owned` in catalog order, refills its ammo, equips if still on fists) and `ownsWeapon(id)`; prices added per weapon in `weapon-catalog.js` ($50 bat → $5000 rocket launcher) via a new `price` field on the `Weapon` base. Wiring: `world.js` reserves the block (park + city-lot loops) and calls `addGunShop(solids)`; `main.js` imports `./gun-shop.js` (registers the interior in `interiors[]`, so touch-to-enter via `doors.js` works automatically) and exposes `refs.gunShopState`/`refs.gunShopBuy`; `hud.js getInteractAction` shows the BUY prompt; `input.js performInteract` calls `refs.gunShopBuy()` on E; gangs avoid the shop facade via the interior's `exterior` zone. The pistol shows as "PISTOL" everywhere (its catalog `name`). Badge bumped to `BUILD 85`. Validation: `node --check` passed for `weapon-types.js`, `weapon-catalog.js`, `weapons.js`, `assets/models/city/gun-shop.js`, `gun-shop.js`, `world.js`, `main.js`, `input.js`, `hud.js`; `npm run build` passed clean (no unresolved imports; only the pre-existing model-viewer glob + chunk-size warnings). Playwright/browser automation not run per the standing instruction.
- Three fixes (request): (1) the "blue block" that randomly cuts into interiors is the SEA — `assets/models/environment/sea.js` is a CircleGeometry of radius 1400 at y=-0.32 centered on the origin, so it passes under the off-map interior cluster (~x=-800) and the third-person camera catches it. Fixed DEFINITIVELY and centrally for any current/future interior: `engine.js` now `export const sea` (was an anonymous `scene.add(makeSea())`), and `js/interior.js` hides it exactly at the state transition — `setSeaHidden(true)` in `Interior.enterAt()` and `setSeaHidden(false)` in `leave()`, the ONLY two places `state.interior` changes, so no per-frame polling and no entry-frame flash; works regardless of where a room is placed. (2) Renamed the gun shop from the trademark-ish "AMMU-NATION" to "AMMO DEPOT" (sign canvas + enter message + comments in `assets/models/city/gun-shop.js` and `js/gun-shop.js`). (3) Renamed `assets/models/weapons/player-gun.js` → `pistol.js` and its factory `makeGunModel` → `makePistolModel` (so the file matches the per-weapon naming pattern), updating importers `weapons.js`, `weapon-catalog.js`, `entities.js`, `doors.js` via `git mv` + rename; repo-wide grep confirms no `player-gun`/`makeGunModel` references remain. Badge bumped to `BUILD 86`. Validation: `node --check` passed for `engine.js`, `interior.js`, `pistol.js`, `gun-shop.js` (model + gameplay), `weapons.js`, `weapon-catalog.js`, `entities.js`, `doors.js`; `npm run build` passed clean. Playwright/browser automation not run per the standing instruction.
- Gun shop: buy CONFIRMATION (request: a confirm-or-not message when buying weapon X, done like the enter/exit-car prompt — NOT a popup modal — and all weapons priced at $1 to test them all). `js/gun-shop.js` now buys in TWO E presses using the existing bottom HUD interact prompt (the same mechanism as "TAKE THE CAR"/"EXIT THE CAR"): the 1st E on a weapon sets a `pending` id and shows `BUY <NAME> FOR $1? PRESS E TO CONFIRM, WALK AWAY TO CANCEL` while `gunShopState()` flips the prompt label to `CONFIRM: BUY <NAME> $1`; the 2nd E on the SAME weapon completes the purchase; walking away (or moving to another weapon) clears `pending` in `updateFx` and shows `PURCHASE CANCELLED` (also cleared on `onExit`). No DOM modal / no game-freeze / no pointer-lock juggling was added — it reuses the interact wiring already in `hud.js getInteractAction` + `input.js performInteract`. All 12 weapon `price` fields in `weapon-catalog.js` set to `1` (temporary TEST pricing so every weapon is affordable; originals are in git history). Badge bumped to `BUILD 87`. Validation: `node --check js/gun-shop.js js/weapon-catalog.js` passed; `npm run build` passed clean. Playwright/browser automation not run per the standing instruction.
- Interior clipping follow-up (user correctly identified the color-changing blue/purple block as sky, not sea): `js/daynight.js` now keeps the sky dome, sun, moon, horizon glow, and star field in one exported sky layer with `setSkyHidden(hidden)`. `js/interior.js` replaced the sea-only hide helper with `setExteriorWorldHidden(hidden)`, hiding both the giant sea disk and the 900-radius sky dome on every `Interior.enterAt()` and restoring both on `leave()`. This fixes the gun shop interior at roughly `(-800,-380)`, whose west/south bounds can cross the sky sphere radius. `js/engine.js` comments were updated to avoid blaming only the sea, and the title badge is now `BUILD 88`. Validation: `node --check` passed for `js/daynight.js`, `js/interior.js`, and `js/engine.js`; `npm run build` passed with only the existing Vite model-viewer dynamic/static import and chunk-size warnings. Playwright/browser automation was not run because the project history records the user's standing instruction not to run Playwright unless explicitly requested.
- Gun shop interior visual upgrade (request: make it feel like a real weapon store): expanded `assets/models/city/gun-shop.js` interior detail while keeping the existing buy positions/confirmation flow unchanged. Added procedural tile/rubber/pegboard textures, glass display cases over the three weapon counters, wall pegboards with mounted arsenal models, ammo/gear shelves with boxed stock, a back service counter with safety glass, cash register, ammo stacks, fluorescent ceiling lights, security cameras, store/rules/test-range signs, and a small range target corner. Moved the clerk behind the new service counter instead of standing in the middle of the back wall. Badge bumped to `BUILD 89`. Validation: `node --check assets/models/city/gun-shop.js`, `node --check js/gun-shop.js`, and `npm run build` all passed; Vite only reported the existing model-viewer dynamic/static import and chunk-size warnings. Playwright/browser automation was not run due the standing no-Playwright-unless-requested instruction.
- Gun shop shooting range room (request: remove the two red TEST RANGE plates, add a small door to a real practice range, put all weapons on the floor for temporary pickup, only one at a time, and return it on exit): removed the old red `TEST RANGE` sign and `RANGE TARGET` plate. Rebuilt the gun shop interior shell as segmented walls, expanded `INT_BOUNDS`, and added an adjacent indoor shooting range through a small auto-opening door at the east end of the shop. The range has rubber flooring, four shooting lanes, safety line, bench/dividers, overhead lane numbers, bullet-trap wall, reactive paper targets, fluorescent lights, and all 12 arsenal weapons placed on floor pickup rings. `js/gun-shop.js` now handles range pickup prompts (`PICK`/`SWAP`), opens the range door by proximity, hides the currently borrowed floor weapon, flashes hit targets, restores the borrowed weapon when leaving the range room, and also restores it on shop exit. `js/weapons.js` added temporary training inventory snapshot/restore (`beginTrainingWeapon`/`clearTrainingWeapon`), saves and restores real ammo/cadence, equips exactly one borrowed weapon plus fists, and routes bullets/missiles to range targets without wanted-level reports inside the range. `main.js` exposes `refs.gunShopTargets` and `refs.inGunShopRange`. Badge bumped to `BUILD 90`; grep confirms no `TEST RANGE`/`RANGE TARGET` strings remain. Validation: `node --check` passed for `assets/models/city/gun-shop.js`, `js/gun-shop.js`, `js/weapons.js`, and `js/main.js`; `npm run build` passed with only the existing Vite model-viewer dynamic/static import and chunk-size warnings. Playwright/browser automation was not run due the standing no-Playwright-unless-requested instruction.
- Gun shop range performance follow-up (request: make the practice room a separate teleported interior instead of an attached heavy room, and improve access to all training weapons): split `assets/models/city/gun-shop.js` into a lightweight visible shop group plus hidden `gunRangeInterior`. The shop now has a solid east wall and only a small `RANGE` portal door; pressing interact near it teleports to an isolated range room with its own bounds/center, exit door, four lanes, targets, bullet trap, lights, safety line, bench, and all 12 arsenal weapons grouped together on floor pickup rings near the entrance. `js/gun-shop.js` now tracks explicit `rangeActive`, swaps visibility between shop/range groups, updates the `Interior` bounds/center before teleporting so the emergency-exit check and camera clamp stay correct, exposes targets/no-wanted only while actually in the range, and returns any borrowed weapon when the player exits back to the shop. Badge bumped to `BUILD 91`. Validation: `node --check` passed for `assets/models/city/gun-shop.js`, `js/gun-shop.js`, `js/main.js`, and `js/weapons.js`; `npm run build` passed with only the existing Vite model-viewer dynamic/static import and chunk-size warnings. Playwright/browser automation was not run due the standing no-Playwright-unless-requested instruction.
- Gun shop range polish (request: internal training-room door needs the same pink entry arrow pattern, training weapon floor is too dark, and shop prices can return to normal): added `gunShopFx.rangeEntryArrow` as a bouncing pink `makeDoorArrow()` just inside the shop's `RANGE` portal and animated it in `js/gun-shop.js`; lightened the training weapon floor mat from near-black to a warm tan so the floor weapons read more clearly; restored non-test weapon prices in `js/weapon-catalog.js` (bat $50, pistol $250, Uzi $800, shotgun $1200, AK47 $2500, M16 $3500, sniper $4000, grenade $300, molotov $250, rocket launcher $5000, flamethrower $4500, detonator $750). Badge bumped to `BUILD 92`. Validation: `node --check` passed for `assets/models/city/gun-shop.js`, `js/gun-shop.js`, and `js/weapon-catalog.js`; `npm run build` passed with only the existing Vite model-viewer dynamic/static import and chunk-size warnings. Playwright/browser automation was not run due the standing no-Playwright-unless-requested instruction.
- Minimap interior-place icons (request: show recognizable icon bubbles for every place with an internal entrance): added optional `mapIcon` metadata to the base `Interior` class and assigned icons/colors for the nightclub, gym, hospital, prison, gun shop, and safe house. `main.js` now exposes `refs.interiorBlips()` from the central `interiors[]` registry and includes it in `render_game_to_text`; `hud.js` draws circular radar markers with canvas pictograms (gun, dumbbell, cross, jail bars, music note, house) at each exterior door, clamped to the radar rim like other blips and hidden during races to preserve race-only radar behavior. Badge bumped to `BUILD 93`. Validation: `node --check` passed for `hud.js`, `main.js`, `interior.js`, `club.js`, `gym.js`, `hospital.js`, `prison.js`, `gun-shop.js`, and `property.js`; `npm run build` passed with only the existing Vite model-viewer dynamic/static import and chunk-size warnings. Playwright/browser automation was not run due the standing no-Playwright-unless-requested instruction.
- Weapon HUD icon pass (request: every weapon needs its own HUD icon, and the gun-shop minimap icon should read more like a pistol): replaced the old two-SVG weapon HUD (`wicon-fist`/`wicon-pistol`) with a `weapon-icon` canvas in `index.html` and updated `css/style.css` sizing for desktop/mobile. `weapons.js getWeaponHud()` now exposes `id`, and `hud.js` draws a distinct canvas pictogram for Fist, Baseball Bat, Pistol, Uzi, Shotgun, AK47, M16, Sniper Rifle, Grenade, Molotov, Rocket Launcher, Flamethrower, and Detonator; it redraws only when the weapon id changes. Also refined the minimap `gun` marker into a clearer pistol silhouette inside the yellow shop blip. Badge bumped to `BUILD 94`. Validation: `node --check` passed for `js/hud.js` and `js/weapons.js`; `npm run build` passed with only the existing Vite model-viewer dynamic/static import and chunk-size warnings. Playwright/browser automation was not run due the standing no-Playwright-unless-requested instruction.
- Weapon HUD spacing fix (request: weapon name text was slightly glued to the weapon icon border): increased the `#weapon-name` vertical offset under the weapon HUD from 3px to 7px on desktop and from 2px to 5px on mobile, preserving the same type size and alignment. Badge bumped to `BUILD 95`. Validation: `npm run build` passed with only the existing Vite model-viewer dynamic/static import and chunk-size warnings. Playwright/browser automation was not run due the standing no-Playwright-unless-requested instruction.
- Melee animation overhaul (request: improve punching and baseball-bat swings): `js/weapons.js` now has explicit melee animation state instead of piggybacking on gun recoil. Punching alternates left/right hands, adds prep/contact/recover timing, body twist, guard hand, a small lunge, and a short swing trail; the baseball bat now has a two-handed windup, contact, recovery, custom held-weapon pose, and a wider golden swing trail. Melee damage is resolved only on the animation impact frame (including range targets), and melee clicks no longer briefly force the gun aiming pose. Badge bumped to `BUILD 96`. Validation: `node --check js/weapons.js` passed; `npm run build` passed with only the existing Vite model-viewer dynamic/static import and chunk-size warnings. Playwright/browser automation was not run due the standing no-Playwright-unless-requested instruction.
