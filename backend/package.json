{
  "name": "tiny-gta-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Leaderboard API (Vercel serverless + Upstash Redis) para Tiny Crime",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "cleanup": "tsx scripts/cleanup.ts"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "@vercel/node": "^5.8.17",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
